version: '3.8'

services:
  db:
    image: "postgres:12"
    environment:
      - "POSTGRES_USER=echome"
      - "POSTGRES_PASSWORD=echome"
      - "POSTGRES_DB=echome"
    volumes:
      - echome_postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
  # web:
  #   build: .
  #   command: python /app/mfeed/manage.py runserver 0.0.0.0:8000
  #   environment:
  #     - DATABASE_URL=postgres://echome:echome@host.docker.internal:5432/echome
  #   volumes:
  #     - .:/code
  #   ports:
  #     - 8000:8000
  #   depends_on:
  #     - db

volumes:
  echome_postgres_data: